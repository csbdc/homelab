apiVersion: v1
kind: Service
metadata:
  name: seerr-prod-media-svc
  namespace: media
spec:
  selector:
    app: seerr
  ports:
    - port: 8096
      targetPort: http
      name: http
      protocol: TCP
