apiVersion: v1
kind: Service
metadata:
  name: dispatcharr-prod-media-svc
  namespace: media
spec:
  selector:
    app: dispatcharr
  ports:
    - port: 9191
      targetPort: http
      name: http
      protocol: TCP
