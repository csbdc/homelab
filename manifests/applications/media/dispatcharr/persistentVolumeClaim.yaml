apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dispatcharr-prod-media-pvc
  namespace: media
  labels:
    app: dispatcharr
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi