apiVersion: v1
kind: Service
metadata:
  name: radarr-prod-media-svc
  namespace: media
spec:
  selector:
    app: radarr
  ports:
    - port: 8096
      targetPort: http
      name: http
      protocol: TCP
