apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qbittorrent-prod-ingest-pvc
  namespace: ingest
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 500Mi
