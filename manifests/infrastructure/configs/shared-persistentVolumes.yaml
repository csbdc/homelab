apiVersion: v1
kind: PersistentVolume
metadata:
  name: ingest-prod-shared-pv
spec:
  accessModes:
  - ReadWriteMany
  capacity:
    storage: 5Gi
  storageClassName: longhorn
  volumeMode: Filesystem
