apiVersion: v1
kind: PersistentVolume
metadata:
  name: ingest-prod-shared-pv
spec:
  accessModes:
  - ReadWriteMany
  capacity:
    storage: 5Gi
  storageClassName: longhorn
  volumeMode: Filesystem
  csi:
    driver: driver.longhorn.io
    fsType: ext4
    volumeHandle: "shared-ingest-media"